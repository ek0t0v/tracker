actor: ApiTester
modules:
    enabled:
        - \App\Tests\Helper\Api
        - Db
        - Asserts
        - Symfony:
              app_path: 'src'
              environment: 'test'
              debug: true
        - Doctrine2:
              depends: Symfony
              cleanup: false
        - REST:
            depends: Symfony
            url: /api
            part: Json
