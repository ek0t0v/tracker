# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
modules:
    enabled:
        - Asserts
        - Tests\Helper\Unit
        - Symfony:
            app_path: 'src'
            environment: 'test'
            debug: true
